// 主题配置
export const theme = {
  // 默认是否开启深色主题
  darkTheme: true,
  // 默认主题色
  appTheme: '#51d6a9',
  appThemeDetail: null
}

// 全局边框圆角
export const borderRadius = '4px'

// 工作区域历史记录存储最大数量
export const editHistoryMax = 100

// 图表初始配置(px)
export const chartInitConfig = {
  x: 50,
  y: 50,
  w: 500,
  h: 300,
  // 不建议动 offset
  offsetX: 0,
  offsetY: 0,
}
